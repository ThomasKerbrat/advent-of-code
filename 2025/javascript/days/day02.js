"use strict";

const { parseFile } = require("../utils/parser.js");

const inputFile = parseFile("../../inputs/day02_sample.txt");
const input = parseInput(inputFile);

function parseInput(inputFile) {
};
